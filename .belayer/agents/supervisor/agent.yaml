schema_version: "1"
description: "Supervisor — orchestrates the ArielCharts team across web, server, and shared packages"
vendor: claude
model: opus
max_turns: 200
max_duration: "4h"
ephemeral: false
workspace: none
belayer_tools:
  - belayer_spawn_agent
  - belayer_request_completion
