schema_version: "1"
description: "Backend implementer — Node 24 + raw HTTP/WS + MCP SDK + LevelDB + Yjs for ArielCharts apps/server"
vendor: claude
model: sonnet
max_turns: 100
max_duration: "2h"
ephemeral: false
workspace: inherit
belayer_tools: []
